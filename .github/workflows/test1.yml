name: Java CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
        
    - name: Build-test
      run: |
        ls -al
        mkdir test-1
        pwd
        touch test01.txt
        ls -al
